{
  "name": "quiver-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ devstar11/quiver-subgraph",
    "create-local": "graph create --node http://localhost:8020/ devstar11/quiver-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ devstar11/quiver-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 devstar11/quiver-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
